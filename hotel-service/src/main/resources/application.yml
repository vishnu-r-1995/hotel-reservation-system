server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:hoteldb
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,metrics,prometheus
